{
  "name": "claude-review",
  "version": "1.0.0",
  "description": "Claude Code agent that reviews PRs and posts structured review comments",
  "main": "dist/index.js",
  "bin": {
    "claude-review": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "diff": "^5.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/diff": "^5.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}